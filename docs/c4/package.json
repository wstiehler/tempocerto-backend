{
  "name": "c4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "new": "c4builder new",
    "config": "cd tempocerto-systems && c4builder config",
    "site": "cd tempocerto-systems && c4builder site",
    "watch": "cd tempocerto-systems && c4builder site --watch",
    "build": "cd tempocerto-systems && c4builder site=false"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "c4builder": "^0.2.16"
  }
}
